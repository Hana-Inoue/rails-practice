<% if current_user.authorized?(controller, action) %>
  <li class="nav-item mb-2">
    <%= link_to t("layouts.menu.links.#{controller}.#{action}"),
                path,
                class: 'nav-link link-dark ps-0' %>
  </li>
<% end %>
